/** @type {import('tailwindcss').Config} */
export default {
    // Tailwind v4 uses CSS-based configuration via @theme in app.css
    // This config file is minimal and only needed for framework integration
    darkMode: "class",
};
